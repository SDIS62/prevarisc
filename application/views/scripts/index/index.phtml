<div class='row-fluid'>
    
    <div class='span3'>
        <h2 class='page-header'>Alertes</h2>
        
          <?php 
            //var_dump($this->etablissementsanspreventionniste);
             foreach($this->etablissementsanspreventionniste as $erpsansprev)
                { ?>
                  <p class='well text-center notification label-warning'>
                  <?php
                       if($erpsansprev['LIBELLE_ETABLISSEMENTINFORMATIONS']!=null)
                       echo "<a href='etablissement/index/id/".$erpsansprev['ID_ETABLISSEMENT']."'> L'établissement : ".$erpsansprev['LIBELLE_ETABLISSEMENTINFORMATIONS']." est sans préventionnistes </a>";
                        ?>      
                  </p>   
        
                <?php 
         
                }
                ?>
            
            <?php
            foreach($this->courrier as $courrier)
                { ?>
                  <p class='well text-center notification label-warning'>
                  <?php
                       if($courrier['DATEREP_DOSSIER']==null)
                       echo "Le courrier/courriel : ".$courrier['OBJET_DOSSIER']." sans réponse depuis plus de 5 jours ";
                        ?>      
                  </p>   
        
                <?php 
         
                }
                ?>
             
            
              <?php
               foreach($this->dossiercommissionechu as $dossier)
                { ?>
                  <p class='well text-center notification label-warning'>
                  <?php
                       if($dossier['OBJET_DOSSIER']!=null)
                       echo "Le dossier : ".$dossier['OBJET_DOSSIER']." de la commission échue : ".$dossier['LIBELLE_DATECOMMISSION']." est sans avis";
                       else
                       {
                           $datecreation = new DateTime($dossier['DATEINSERT_DOSSIER']);
                           echo "Le dossier du ".$datecreation->format('d/m/y')." (".$dossier['LIBELLE_DOSSIERTYPE'].") de la commission échue : ".$dossier['LIBELLE_DATECOMMISSION']." est sans avis";
                          
                           
                       }
                       ?>      
                  </p>   
        
                <?php 
         
                }
                ?>
                
          
          
               <?php
                foreach($this->etablissementavisdefavorable as $avis)
                { ?>
                  <p class='well text-center notification label-info'>
                  <?php 
                       echo "L'établissement ".$avis['LIBELLE_ETABLISSEMENTINFORMATIONS']." est sous un avis défavorable depuis dix jours";
                  ?>      
                  </p>   
        
                <?php 
         
                }
                ?>
       
    </div>

    <div class='span3'>
        <a class='btn btn-small pull-right' style='margin-top: 5px' data-toggle="modal" data-target="#reponse-modal" href='<?php echo $this->url(array('controller' => 'news', 'action' => 'add')) ?>'>Ajouter un message</a>

        <h2 class='page-header'>Derniers messages</h2>
        <?php if( count($this->flux) == 0 ) : ?>
            <p class='muted'><small>Aucun message disponible.</small></p>
        <?php else : ?>
            <table class='table table-hover table-condensed'>
             <?php echo $this->partialLoop('news/partials/partial-display.phtml', $this->flux); ?>
            </table>
        <?php endif ?>
    </div>
    
    <div class='span3'>
        <h2 class='page-header'>Prochaines commissions</h2>
 
     <?php  
        
     
     if($this->firstcommission!=NULL)
     {   
         foreach($this->commissionsListe as $commission)
         {
               foreach($this->firstcommission as $commissiondujour)
             {             
                if($commission['ID_COMMISSION']==$commissiondujour['COMMISSION_CONCERNE'])
                {
                    if ($commissiondujour['ID_COMMISSIONTYPEEVENEMENT'] == 1) {
                        $class="label-info";
                    } else if ($commissiondujour['ID_COMMISSIONTYPEEVENEMENT'] == 2) {
                        $class="label-success";
                    } else {
                        $class="label-warning";
                    }
                ?>
       <p class='well text-center notification <?php echo $class ?>'>   
            <a href="/calendrier-des-commissions/gestionodj/dateCommId/<?php echo $commissiondujour['ID_DATECOMMISSION'];?>"
            >
                <?php
                
                   $date = new DateTime($commissiondujour['DATE_COMMISSION'].' '.$commissiondujour['HEUREDEB_COMMISSION']);
                   echo "Le ".$date->format('d/m à H:i');
                   echo " : ".$commission['LIBELLE_COMMISSION'];
                   echo '-'.$commissiondujour['LIBELLE_DATECOMMISSION'];
                ?> 
             
            </a> 
       </p>
                    
                <?php
                }   
             }  
         }
     }
     else
     {
      ?>
     <p class='well text-center pasnotification'>
       <a>
     <?php
         echo 'Aucune commission'; 
     ?>
       </a>
     </p>
     <?php
     }    
     ?>
                    
    </div>

</div>

<h2 class='page-header'>
    Tableau de bord
    <small>Profil : <?php echo $this->user['infos']['LIBELLE_FONCTION'] ?></small>
</h2>

<h3>Vos établissements</h3>
<?php if( count($this->etablissements) == 0 ) : ?>
    <p class='muted'><small>Aucun établissement suivi.</small></p>
<?php else : ?>
    <ul class='recherche_liste unstyled no_slide'>
    <?php echo $this->partialLoop('search/results/etablissement.phtml', $this->etablissements ) ?>
    </ul>
    <br/>
    <div style='clear: both'></div>
    <?php echo $this->etablissements ?>
<?php endif ?>

<div id="reponse-modal" class="modal hide fade" ></div>

<script type="text/javascript">
    $(document).ready(function() {
        // Gestion des boites modales
        $("a[data-toggle='modal']").click(function() {
            var target = $(this).attr("data-target");
            var url = $(this).attr("href");
            $(target).load(url);
        });
    });
</script>