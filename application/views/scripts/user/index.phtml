<!-- Titre -->
<h2 class='page-header'>Administration système</h2>

<?php echo $this->partial('menu_admin.phtml') ?>

<?php echo $this->partial('menu_admin_user.phtml') ?>

<div class='row-fluid'>

    <div class="tabbable tabs-left span2">
        <ul class='nav nav-tabs' style='width: 100%'>
            <li <?php if(!isset($_GET['gid'])) echo "class='active'" ?>><a href='/user' >Tous les utilisateurs</a></li>
            <?php foreach($this->groupes as $groupe) : ?>
            <li <?php if(isset($_GET['gid']) && $_GET['gid'] == $groupe['ID_GROUPE']) echo "class='active'" ?> >
                <a href='/user?gid=<?php echo $groupe["ID_GROUPE"] ?>' ><?php echo $groupe["LIBELLE_GROUPE"] ?></a>
            </li>
            <?php endforeach ?>
        </ul>
    </div>

    <div id='content_group' class='span10'>
    
        <a href='/user/add-group?gid=<?php echo $_GET['gid'] ?>' class='btn pull-right' >Modifier le groupe</a>

        <!-- Liste des utilisateurs -->
        <?php if($this->users != null) : ?>
        <table class='table table-condensed table-striped'>

            <thead>
                <tr>
                    <th>Identifiant</th>
                    <th>Nom et prénom</th>
                    <th>Fonction</th>
                    <th style='width: 150px' >Etat</th>
                    <th style='width: 150px' >Actions</th>
                </tr>
            </thead>
            
            <tbody>
                <?php foreach( $this->users as $user ) : ?>
                <tr <?php if(!$user["ACTIF_UTILISATEUR"]) echo "style='color:silver'" ?> >
                    <td style='font-style: italic' ><?php echo $user["USERNAME_UTILISATEUR"] ?></td>
                    <td><a href='/user/profile?uid=<?php echo $user["ID_UTILISATEUR"] ?>' ><?php echo $user["NOM_UTILISATEURINFORMATIONS"] . " " . $user["PRENOM_UTILISATEURINFORMATIONS"] ?></a></td>
                    <td><?php echo $user["LIBELLE_FONCTION"] ?></td>
                    <td>
                        <?php
                        echo ( time() - strtotime($user["LASTACTION_UTILISATEUR"]) < ini_get('session.gc_maxlifetime') ) ? "<span style='color: #008C00; font-weight: bold'>Connecté</span>" : "<span style='color: silver'>Déconnecté</span>"
                        ?>
                    </td>
                    <td>
                        <a href='/user/edit/uid/<?php echo $user["ID_UTILISATEUR"] ?>' class="edit">Modifier</a> ou 
                        <a class='act_deact' href='/user/actived?uid=<?php echo $user["ID_UTILISATEUR"] ?>' ><?php echo ($user["ACTIF_UTILISATEUR"]) ? "Désactiver" : "Activer" ?></a>
                    </td>
                </tr>
                <?php endforeach ?>
            </tbody>
            
        </table>

        <?php else : ?>
        <p class='lead' ><strong>Aucun résultat.</strong></p>
        <?php endif ?>
    </div>

    <div id='dialog-add-groupe'></div>
    
</div>


<script type="text/javascript">

	
	function dialog_groupe( gid ) {

		$('#dialog-add-groupe').dialog("destroy");
	
		$("#dialog-add-groupe").dialog({
			title: gid ? "Modification du groupe" : "Ajout d'un groupe",
			height: 300,
			width: 650,
			draggable: false,
			resizable: false,
			create: function() {
				$('#dialog-add-groupe').load( "/user/add-group" + ( gid ? "?gid=" + gid : "" ) );
				$("#dialog-add-groupe input").val("");
			},
			buttons: {
				"Sauvegarder" : function() {
					$.ajax({
						url: "/user/save-group?format=json",
						data: $("#formulaire_groupe").serialize(),
						type: "POST",
						success: function () {
							window.location.reload() ;
						}
					});
				},
				Cancel: function() {
					$('#dialog-add-groupe').dialog("destroy");
				}
			}
		});
	}

</script>